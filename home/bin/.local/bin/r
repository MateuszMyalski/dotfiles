#!/usr/bin/env bash
if [ $# -eq 0 ]
  then
    echo "Usage: $(basename $0) [FILE]"
    exit 1
fi

if command -v batcat &>/dev/null; then
    exec batcat $@
elif command -v cat &>/dev/null; then
    exec cat $@
fi